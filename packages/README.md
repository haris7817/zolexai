# packages

Shared libraries. Populated from M1 onward:

- `ui/` shared primitives
- `web-config/` shared lint/TS config
- `api-client/` typed client generated from OpenAPI (M1.16)
- `workflow-contracts/` shared workflow types
